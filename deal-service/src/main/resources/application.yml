spring:
  datasource:
    url: jdbc:postgresql://db:5432/db01
    username: postgres
    password: qwerty123
    driver-class-name: org.postgresql.Driver

  jpa:
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    hibernate:
      ddl-auto: validate

  liquibase:
    enabled: true
    change-log: classpath:db/changelog/db.changelog-master.xml
    default-schema: public


  kafka:
    bootstrap-servers: kafka:29092

springdoc:
  swagger-ui:
    path: /swagger-ui.html
    enabled: true
  api_docs:
    enabled: true

server:
  port: 8081

scoring:
  service:
    url: http://calculator-service:8080/calculator/calc

pre-scoring:
  service:
    url: http://calculator-service:8080/calculator/offers

